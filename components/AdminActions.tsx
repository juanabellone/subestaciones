'use client'

// Placeholder para futuras acciones del admin (agregar subestaciones, DVRs, usuarios)
export default function AdminActions() {
  return (
    <div className="flex gap-2">
      <span className="text-xs text-gray-500 italic">Gestión desde Supabase Dashboard por ahora</span>
    </div>
  )
}
